# Admin extensions

Dashboard widgets and routes for the Base.com integration live here.

The directory has to exist even while empty: the plugin build writes its
generated extension manifest into it and fails outright if it is missing.
